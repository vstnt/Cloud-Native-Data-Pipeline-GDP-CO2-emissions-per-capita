# Copy this file to ".env" and fill in values.
# Do NOT commit ".env"; it is ignored by .gitignore.

##### Local run (optional overrides) #####
# WORLD_BANK_INDICATOR=NY.GDP.PCAP.CD
# WIKIPEDIA_URL=https://en.wikipedia.org/wiki/List_of_countries_by_carbon_dioxide_emissions_per_capita
# Path to the local JSON metadata file (default: local_metadata.json)
# METADATA_LOCAL_FILE=local_metadata.json

##### Cloud pipeline configuration #####
# Required for cloud (S3 + DynamoDB adapters)
PIPELINE_S3_BUCKET=your-bucket-name
# Optional logical base prefix inside the bucket
PIPELINE_S3_BASE_PREFIX=gdp-co2-pipeline
PIPELINE_METADATA_TABLE=env_econ_pipeline_metadata

# AWS region used by AWS CLI, ECR and stack deployment
AWS_REGION=us-east-1

##### Cloud deployment (build_and_deploy.sh) #####
# Optional: customize names and schedule
STACK_NAME=gdp-co2-pipeline
STACK_SUFFIX=gdp-co2
ECR_REPO=gdp-co2-pipeline
# IMAGE_TAG can be left blank; script defaults to current timestamp
# IMAGE_TAG=latest

# EventBridge schedule (default: daily at 02:00 UTC)
SCHEDULE_EXPRESSION=cron(0 2 * * ? *)

# Lambda sizing
MEMORY_SIZE=2048
TIMEOUT=900

# Optional resource creation flags for CloudFormation template
# If 'true', stack will create and RETAIN these resources; if 'false', use existing ones
CREATE_S3_BUCKET=true
CREATE_DYNAMO_TABLE=true

